# --- !!config.DB
DB:
  type: mysql
  # version: 8.0
  address: 127.0.0.1
  port: 3306 #default mysql 3306
  database: test
  # driver: io
  # url: jdbc:mysql://${mysql.address:localhost}:{mysql.port:3306}/{mysql.table}&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  # url: 
  user: root
  passwd: 123456

